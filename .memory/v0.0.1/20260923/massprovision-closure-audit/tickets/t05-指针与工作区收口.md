# t05 指针与工作区收口

**状态**：✅ 已完成

| 项 | 处理 |
| --- | --- |
| 总控 pin `cloud` = `0feebda`，而 cloud 远端/工作区已是 `a5ebe30`（+3：Spring Cloud 后端 + oneye-web 管理端上传） | 在总控提交 `cloud` 新 gitlink（并把 `cloud/` 写进仓库地图与索引 26） |
| `backend` 未跟踪残留：`claim_handler.exe`(27 MB)、`src/test/mock/wwww.txt`、`alexa_skill.txt`、`mcp_tests.txt`、`mcp_tools_tests.txt` | 删除（都是测试计时残留/Windows 构建产物；`docs/practice/测试与自动化.md` 早已记过"清理后勿提交"） |
| 防复发 | `backend/.gitignore` 增 `/src/**/*.txt`（已核实 `git ls-files 'src/**/*.txt'` = 0）与 `/*.exe`；`git check-ignore -v` 实证命中 |
| `backend/src/rmneo/credsource/` 原为未跟踪草稿 | 转为正式入库（见 t01） |

**核对**：三仓 + SDK/index/cloud 的 `HEAD ↔ origin` 均为 `0/0`（无未推送提交）；总控工作区在提交后应只剩本轮改动。
