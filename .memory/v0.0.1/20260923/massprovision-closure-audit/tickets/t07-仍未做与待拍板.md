# t07 仍未做 / 待拍板（本轮**不**声称完成）

**状态**：⏳ 登记（未开始；等拍板或排期）

## 待拍板（设计 §8.6）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | 私钥在哪生成 | 两条路都支持（接口 `items[].csr`）；当前默认"服务端生成"（ZIP 带 `client.key`），代价是私钥在服务端日志/临时文件/备份里出现过 |
| 4 | 是否做 eFuse 防克隆 | 不做则"复制 flash = 复制身份"；伙伴掌握整片 flash 时靠吊销兜底 |
| 5 | 凭证分区是否加密（flash/内容加密） | 与 eFuse、烧写工装同批决定 |
| 6 | 产线是否允许联网 | 当前建议：不允许（办公室一次做完，产线只写分区） |

## 未做（已登记，不阻塞本批）

- 产线工装/扫码/一拖多/PASS-FAIL 上传（当前形态 = "一台一把线、跑一条命令"）。
- 存量重签迁移（设计 §3.6.1；属排期项）。
- **shadowd 生产部署**：上线必须显式配 `DEVICE_CRL_FILE`/`DEVICE_CRL_URL`（`DEVICE_CRL_REQUIRED=1` 可做硬约束）。
- EMQX **authentication**（ACL 只管授权；`username` 仍是自述 ⇒ 知道 node_id 即可冒用）。
- 申领服务自身 TLS/mTLS（当前明文 HTTP + 控制台令牌）。
- 台账按 `node_id` 的专用索引（量大再加 `(node_id) WHERE revoked_at IS NULL`）。
- 批量签发**不写设备注册表**（量产设备首次上线仍走设备自己的上报路径）。

## 口径边界（别读成"已解决"）

- 指纹兜底默认**关闭**（`DEVICE_COMMON_CERT_FINGERPRINTS` 为空即不参与），且需有人在部署侧登记公用证书指纹。
- shadowd 的"当前用公用身份节点集合"按节点数增长（单实例形态未加淘汰）。
- 量产固件（`sdkconfig.defaults.production`）本轮只做到**构建级 + 配置生效级**证据，**未上板**；上板复验用的是台面口径构建。
