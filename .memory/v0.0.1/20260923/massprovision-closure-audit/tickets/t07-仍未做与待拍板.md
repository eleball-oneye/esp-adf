# t07 仍未做 / 待拍板（含 2026-09-23 拍板结果）

**状态**：本轮涉及的三项已拍板并回写；**私钥与控制台入口也已拍板并落地**（见 t08）

## 已拍板（2026-09-23，已回写三仓文档）

| 项 | 决定 | 回写落点 |
| --- | --- | --- |
| 防克隆（eFuse） | **不做** —— 明确接受"复制整机 flash 即复制身份"，**靠吊销兜底** | 设计 §8.4/§8.6 #4、契约 §7 红线 5、作业指导 §5-3/§7 #4、index 量产块、总控可行性评估/落地计划 |
| 凭证分区加密 | **不做** —— 我们自己的量产形态不启用 flash/内容加密，`creds` 明文写入；**伙伴若自行开 flash 加密，"是否标 `encrypted` 须双方确认"仍有效** | 设计 §8.6 #5、契约 §1 表 + §7 红线 4、作业指导 §7 #5 |
| 产线是否联网 | **联网**，且**批量申领在控制台 web 上操作** | 设计 §8.6 #6、作业指导 §5-4/§7 #6 与"三道工序"、index、总控两处 |
| **私钥在哪生成** | **服务端生成**（A 路）—— 交付 ZIP 带 `client.key`；**归档按密钥介质管理**；将来改"只递 CSR"不改接口 | 设计 §8.6 #1 / §8.8.1、契约 §7 红线 6、作业指导 §1.2/§7 #1、index、总控两处 |
| **控制台批量签发入口** | **现在做，且已落地** | 设计 §8.6 #8 / §8.7.5 / §8.8.2、作业指导 §1.2/§7 #6a、index、总控两处（详见 t08） |

## 由此新增的缺口（待拍板要不要现在做）

见 t08；结论已落：入口已实现，**仍未做**的是联网产线的三条上线前置（产线专用账号/最小权限、申领服务自身 TLS、令牌吊销/续期）与 `CORS_ORIGINS` 收紧。

## 未做（登记，按需排期）

产线工装/扫码/一拖多/PASS-FAIL 上传（按产量取舍）、存量重签迁移（放量前那次做）、
**shadowd 生产部署**（含 `DEVICE_CRL_*`；不部署则身份告警虽已实现但没人消费）、
EMQX **authentication**（ACL 只管授权，username 仍是自述 ⇒ 生产前必做）、
`esp.cred_source` 身份视图列表页（服务端判决/告警已落，控制台暂无列表）、
台账 `node_id` 专用索引（量大再加）。

