# t01 真机端到端：把 B-20260923-192813 的证书烧进 Korvo-2 并验证上线

**状态**：✅ 完成（上线判据通过；控制台可见性卡在 t05 的问题 3）

## 做了什么

- `mkcreds -zip claim-batch-B-20260923-192813.zip -out E:\tmp\prd-test\creds-B-20260923-192813`
  ⇒ `KORVO2-0000\creds.bin` 16384 B，CRC32 `cac3238f`，清单 `status=issued`。
- 端点选择：拿本批 `client.crt/client.key/ca.crt` 对 4 个 mTLS 端口跑 `openssl s_client`：
  **18886 `Verification: OK`**；18885/18884/8883 `self-signed certificate in certificate chain`。
- 固件：量产预设 + `CONFIG_ONEYE_FW_CLOUD_PORT=18886` +（台面）`ENABLE_WIFI_FILE=y`，
  `ONEYE_DEV_CREDS_REQUIRED=y`、不嵌证书 ⇒ `output/.build/korvo2_oneye-platca18886/`。
- 烧写：`provision-device.ps1 -Port COM12 -Sn KORVO2-0000 -CredsDir … -BuildDir … -LogFile …`
  ⇒ 退出码 **0**，固件 4 段 + 凭证 1 段全部 `Hash of data verified`，回读 16 KB 逐字节一致。

## 判据与实测

| 判据 | 实测 |
| --- | --- |
| 串口身份来自分区 | `creds 分区 … node=KORVO2-0000 cert 652 B / key 227 B / CA 595 B` |
| 自证串与清单一致 | `ONEYE-PROV1 node_id=KORVO2-0000 cert_fp=56c4e7aa8c775b17…`（= manifest 的 `fingerprint_sha256`） |
| 身份来源 | `esp.cred_source=partition` |
| 端点可达 | `net_probe: cloud endpoint mqtt.oneye.me:18886 → OK` |
| 链路建立 | `link up: mqtt.oneye.me:18886 transport=mqtt-tls node=KORVO2-0000` |
| 门卫侧身份 | `Client(KORVO2-0000, username=KORVO2-0000, connected=true)`（ACL 放行自己的 topic） |
| 平台应答 | `time_sync → rmng/dev/KORVO2-0000/caps/down cloud_ts=…`、`ack → …/log/down code=ok items=1` |
| 台账 | `device_certs` 有本批一行（serial/指纹/`not_after 2036-09-17`） |

## 未通过 / 未证明

- `GET /v1/devices` 不含 `KORVO2-0000`；`GET /v1/devices/KORVO2-0000/shadow` = **404**；
  身份视图为空 —— 原因 = 设备没发 `shadow/up`（t05 问题 3），**不是**证书问题。
- 串口中文提示在采集侧是 `?`（固件串口输出把非 ASCII 变成 `?`），判据一律用 ASCII 行。
- 抓串口必须带一次复位（`-Reset`）：单纯打开端口时板子可能不吐日志，看起来像"板子死了"。
